{
  "hilScript": "hil/test-user-windows.ps1",
  "evidenceMode": "snapshot",
  "requireVisualExpectations": true,
  "context": [
    "Resume the existing feature/user-windows worktree and preserve all of its uncommitted implementation, example, documentation, and HIL-test changes.",
    "A prior physical run compiled and uploaded successfully. Window definition, selection, independent population, and updating window 1 while preserving window 2 were observed working.",
    "The prior final camera capture restored the base window but materially failed the intended layout: it showed BASE ACTIVEWINDOWS on the first row and OFF wrapped below, instead of BASE ACTIVE above WINDOWS OFF. Treat that prior PASS as a false positive.",
    "Do not return pass unless the focused HIL script supplies exact per-image visual expectations and every attached capture matches its required rows, placement, wrapping, and absence criteria."
  ]
}
